#include <iostream>
#include "Jugador.h"
#include "Reglas.h"
using namespace std;

void Jugador::Posicion(){
	Reglas* regla = new Reglas();
	bool bandera = true;
	string pos;
	/*while(bandera){
		cout<<"Ingrese La Posicion de la Ficha que Desea Mover: ";
		cin>>pos;
		bandera = regla->MoverActual(pos);
		cout<<"Ingrese La Posicion donde Desea Moverla: ";
		cin>>pos;
		bandera = regla->MoverFuturo(pos);
		
	}*/
}
